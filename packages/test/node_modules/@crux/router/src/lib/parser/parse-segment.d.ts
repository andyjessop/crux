import { RouteParams } from '../router';
/**
 * Parse a segment, returning a decodeURL function.
 */
export declare function parseSegment(seg: string): (str: string, paths: RouteParams, array?: boolean) => boolean;
