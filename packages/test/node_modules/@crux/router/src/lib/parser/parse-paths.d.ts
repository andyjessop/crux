import { RouteParams } from '../router';
/**
 * Parse a path, returning a curried function.
 */
export declare function parsePaths(targets: string[]): (path: string[], params: RouteParams) => boolean;
