"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.paramsToStrings = void 0;
function paramsToStrings(dict) {
    for (const key in dict) {
        // eslint-disable-next-line
        if (dict.hasOwnProperty(key)) {
            if (Array.isArray(dict[key])) {
                for (let i = 0; i < dict[key].length; i++) {
                    dict[key][i] = dict[key][i].toString();
                }
            }
            else {
                dict[key] = dict[key] ? dict[key].toString() : null;
            }
        }
    }
    return dict;
}
exports.paramsToStrings = paramsToStrings;
//# sourceMappingURL=params-to-strings.js.map