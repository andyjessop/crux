"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.escapeRegexes = void 0;
const constants_1 = require("./constants");
/**
 * Escape named params from a pattern.
 */
function escapeRegexes(pattern) {
    const match = pattern.match(constants_1.namedParamRegex) || [];
    for (let i = 0; i < match.length; i++) {
        const m = match[i];
        const regex = m.slice(m.indexOf('<') + 1, m.length - 1);
        // eslint-disable-next-line no-param-reassign
        pattern = pattern.replace(regex, encodeURIComponent(regex));
    }
    return pattern;
}
exports.escapeRegexes = escapeRegexes;
//# sourceMappingURL=escape-regexes.js.map