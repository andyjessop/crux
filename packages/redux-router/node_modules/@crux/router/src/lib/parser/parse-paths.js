"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parsePaths = void 0;
const parse_segment_1 = require("./parse-segment");
/**
 * Parse a path, returning a curried function.
 */
function parsePaths(targets) {
    const parsers = targets.map(parse_segment_1.parseSegment);
    return function curriedParsePaths(path, params) {
        if (targets.length !== path.length) {
            return false;
        }
        for (let i = 0; i < targets.length; i++) {
            if (!parsers[i](path[i], params)) {
                return false;
            }
        }
        return true;
    };
}
exports.parsePaths = parsePaths;
//# sourceMappingURL=parse-paths.js.map